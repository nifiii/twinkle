## **拍题页面导航与布局深度优化方案**

根据您的最新需求，我将调整“拍题”页面的默认进入状态，并重构顶部导航栏，使其能够快速切换“拍题”、“错题”与“归档”三个核心功能。

---

### **1. 默认状态调整 ([CaptureModule.tsx](file:///d:/devops/HL-os/components/CaptureModule.tsx))**
- **默认聚焦采集**：将 `activeSubTab` 的初始状态从 `'portal'` 修改为 `'capture'`。
- **效果**：用户点击侧边栏的“拍题”按钮后，将直接进入“开始数字化采集”的引导页面，无需经过门户选择页，减少操作路径。

---

### **2. 顶部导航栏重构 ([CaptureModule.tsx](file:///d:/devops/HL-os/components/CaptureModule.tsx))**
- **整合入口按钮**：在顶部的白色操作栏中，紧贴“返回主页”按钮，增加两个常驻的入口按钮：
    - **[错题本]**：点击切换到错题列表视图。
    - **[归档文件]**：点击切换到数字化档案库视图。
- **交互逻辑**：
    - 当前所在的页面按钮将呈现高亮状态（Primary 样式），其他入口呈现次要状态（Outline 样式）。
    - 确保在“采集”、“错题”、“归档”三个子页面之间切换时，导航栏始终保持一致且可见。

---

### **3. 布局细节优化**
- **对齐与间距**：参考您提供的截图，将“返回主页”、“错题本”、“归档文件”三个按钮并排排列在左侧，中间保持“立即拍题”或当前子页面标题的展示。
- **响应式适配**：确保在窄屏下，按钮组能合理折行或缩减文字，保持 UI 整洁。

---

### **4. 实施步骤**
1.  **修改 [CaptureModule.tsx](file:///d:/devops/HL-os/components/CaptureModule.tsx)**：
    - 更新 `useState` 初始值。
    - 重构 `activeSubTab !== 'portal'` 逻辑块内的 Header 部分，插入新的按钮组。
    - 调整“返回主页”按钮的逻辑（如果需要直接返回 Dashboard，或者保留作为返回拍题门户的逻辑）。
2.  **样式微调**：确保新增按钮的视觉权重与现有 UI 风格统一。

请确认以上优化方案，确认后我将立即执行修改。