## 1. 后端路由完善 (Backend)
- 修改 [books.ts](file:///d:/devops/HL-os/backend/src/routes/books.ts)，添加 `DELETE /api/books/:id` 路由接口。
- 在路由处理函数中调用 [fileStorage.ts](file:///d:/devops/HL-os/backend/src/services/fileStorage.ts) 的 `deleteMetadata` 方法，确保数据库记录和物理文件同步删除。

## 2. 前端服务层封装 (Frontend Service)
- 修改 [apiService.ts](file:///d:/devops/HL-os/services/apiService.ts)，导出 `deleteBook(id: string)` 函数，封装对后端 `DELETE` 接口的调用。

## 3. 前端界面对接 (Frontend UI)
- 修改 [LibraryHub.tsx](file:///d:/devops/HL-os/components/LibraryHub.tsx) 的 `handleDeleteBook` 函数：
    - 移除模拟逻辑和 `TODO` 注释。
    - 调用 `apiService.deleteBook` 进行真实删除。
    - 删除成功后，调用 `loadBooks()` 刷新列表并切换视图。

## 4. 验证与测试
- 在界面执行删除操作，观察网络请求。
- 检查服务器日志确认 SQLite 记录 (`DELETE FROM books ...`) 和物理文件 (`fs.unlink`) 的删除动作。
- 确认页面列表正确刷新。