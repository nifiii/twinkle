# 智学专家级数字孪生方案实施计划

## 1. 模型与识别逻辑升级
- **模型切换**：将 `backend/src/routes/analyze.ts` 中的模型升级为 `gemini-3-pro-preview`，以获取更强的版面分析能力。
- **协议强化**：更新 `OCR_SYSTEM_INSTRUCTION`，明确要求 AI 捕获所有印刷信息（含页眉、任务指导、背景材料、插图描述），并采用双流（还原流+判定流）输出。

## 2. 存储路径与数据库重构
- **路径规范化**：修改 `backend/src/services/fileStorage.ts`，将图片和 Markdown 的存储路径改为：
  - 图片：`originals/Exams_Homework/YYYY-MM-DD/Subject/Student/`
  - 错题：`obsidian/Wrong_Problems/YYYY-MM-DD/Subject/Student/`
- **数据库增强**：在 SQLite `scanned_items` 表中持久化存储全量题目 JSON 数据，建立错题与原卷的强关联索引。

## 3. 档案库联动视图开发
- **分类展示**：更新 `components/KnowledgeHub.tsx`，按科目和日期对试卷进行聚合展示。
- **溯源查看器**：实现一个新组件，点击错题后可同时并排查看“原始试卷图片”与“数字化解析内容”。

## 4. 流程验证
- 上传包含复杂背景（如漫画阅读理解）的试卷，验证识别完整性。
- 检查服务器目录结构是否符合 `Time/Subject/Student` 范式。
- 验证从档案库点击错题能否正确定位到原卷。

**确认后我将按此计划执行代码修改。**