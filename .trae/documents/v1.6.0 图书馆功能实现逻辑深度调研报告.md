## 1. 恢复 v1.6.0 丢失的 AI 元数据与转换功能 (Priority: High)
- **后端修复**: 
    - 修改 [upload-book.ts](file:///d:/devops/HL-os/backend/src/routes/upload-book.ts)：在 `/api/upload-book/parse` 路由中，将 `tableOfContents`、`publisher`、`publishDate` 加入返回结果。
    - 优化 [saveBook.ts](file:///d:/devops/HL-os/backend/src/routes/saveBook.ts)：确保异步 `convertToMarkdown` 流程闭环，增加转换失败的日志捕获。
- **前端恢复**:
    - 修改 [BookMetadataModal.tsx](file:///d:/devops/HL-os/components/BookMetadataModal.tsx)：新增章节目录预览 UI，展示 AI 提取的 TOC。
    - 修改 [BookUploader.tsx](file:///d:/devops/HL-os/components/BookUploader.tsx)：确保 `handleSaveMetadata` 时完整保留并传递 `tableOfContents`。

## 2. 安全集成删除功能 (Safe Implementation)
- **后端开发**:
    - 在 [books.ts](file:///d:/devops/HL-os/backend/src/routes/books.ts) 中添加 `DELETE /api/books/:id` 路由。
    - 增强 [fileStorage.ts](file:///d:/devops/HL-os/backend/src/services/fileStorage.ts) 的 `deleteMetadata`：支持物理删除原始 PDF、Markdown 和封面图。
- **前端对接**:
    - 修改 [apiService.ts](file:///d:/devops/HL-os/services/apiService.ts) 增加 `deleteBook` 调用。
    - 修改 [LibraryHub.tsx](file:///d:/devops/HL-os/components/LibraryHub.tsx) 对接真实删除逻辑，并同步清理 IndexedDB 缓存。

## 3. 全链路验证
- 执行完整的“上传-识别-保存-转换-阅读-删除”流程，确保新旧功能互不干扰。