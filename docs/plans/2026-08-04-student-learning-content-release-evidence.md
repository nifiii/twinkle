# 学生自学内容发布前验证记录（T-008）

状态：本地验证通过；T-009 生产清理与上线仍需单独人工授权

## 1. 背景

本记录为学生自学课件、统一错题本与学习助手全量清理能力的发布前本地证据。验证环境为隔离的 Node 22 容器 `t008-backend`（`127.0.0.1:3001`）和本地 Vite 服务（`127.0.0.1:5176`）；未读取或写入生产数据库、对象存储或部署环境。

## 2. 目标

- 验证 T-001 至 T-007 的后端、前端、统一错题本浏览器路径和清理 dry-run。
- 证明归档错题与课堂作答错题保持各自原有详情和权限边界。
- 给出 T-009 是否具备进入人工生产授权环节的依据。

## 3. 方案与证据

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 环境重启 | 通过 | 隔离后端容器已重启；Vite 以临时代理配置重新启动，`/api` 指向 `127.0.0.1:3001`。 |
| 后端回归 | 通过 | Node 22 容器内 `npm test`：67 passed，0 failed。覆盖清理 dry-run/apply、学生自学课件、统一错题本聚合、筛选与部分来源不可用。 |
| 前端构建 | 通过 | `npm run build` 完成，TypeScript 与 Vite 均无错误。 |
| 清理 dry-run | 通过 | `learningAssistantReset.ts --dry-run` 生成数据库/metadata SHA-256 备份和 manifest，`blockers: 0`；临时夹具中仅有一条课堂成绩候选，未执行 `--apply`。保留教材/试卷的正反例由已通过的清理集成测试覆盖。 |
| 统一错题本 API | 通过 | `GET /api/wrong-book?ownerId=child_1` 同时返回 `scanned_item` 与 `quiz_result`；删除归档夹具后只保留课堂来源，证明删除沿用原扫描归档路径且未影响课堂作答记录。 |
| 归档错题交互 | 通过 | 页面显示“查看错题”“删除归档错题”；查看打开既有详情，删除有不可恢复确认，确认后条目从 API 与页面消失。 |
| 课堂错题交互 | 通过 | “查看测验详情”进入 `#tutor/task/legacy:quiz_result:<id>`，展示原测验成绩、逐题答案、解析和人工改判入口；页面无扫描错题删除入口。 |
| 三视口与键盘 | 通过 | 390、768、1440 宽度下筛选、来源标签和主操作未重叠；“指定日期”展开后键盘 Tab 可到达开始日期输入。截图：`output/playwright/t008-unified-wrong-book-default-390.png`、`output/playwright/t008-unified-wrong-book-default-768.png`、`output/playwright/t008-unified-wrong-book-default-1440.png`。 |

### 评审结论

- Spec：通过。统一错题本只读聚合两种来源；归档来源保留查看/删除，课堂来源只进入成绩详情，符合已确认的范围。
- UI/UX：通过。三视口未见文字、标签、筛选器或主操作重叠；移动端筛选区按页面纵向滚动展示。
- 代码质量：功能回归通过。GitNexus 文本索引当前提示 FTS 缺失，未作为功能正确性依据；源代码与测试直接验证完成。最终变更扫描识别到当前未提交集合同时覆盖 T-001 至 T-007 的 66 个符号和 25 条执行路径，发布治理风险为 `CRITICAL`，因此 T-009 前必须完成范围复核和原子提交。

## 4. 风险

| 风险 | 影响 | 处理 |
| --- | --- | --- |
| 前端主包约 1.60 MB（gzip 约 524 KB） | 弱网首次加载可能偏慢 | 非本次功能阻断项；后续性能任务评估路由级分包，不在 T-008 改动。 |
| 多任务实现仍在同一未提交工作区 | 无法将发布变更范围稳定映射到单一任务，直接进入生产会放大回滚风险 | 发布前按 T-001 至 T-008 复核范围，执行 GitNexus 影响检查并以原子提交固化；在此之前不得进入 T-009。 |
| 本地 dry-run 使用隔离夹具 | 不构成生产数据已清理证明 | T-009 前必须在维护窗口生成生产 dry-run manifest，并由人工核对后再获得单次 `--apply` 授权。 |
| 临时验证容器包含生成的备份与夹具 | 若长期保留会占用本机资源 | T-008 收尾时停止并删除临时容器与 Vite 进程；不影响仓库或生产数据。 |
